## 5. Entity Relationship Diagram (ERD)

```mermaid
erDiagram
    USERS ||--o| SISWA : "memiliki profil"
    USERS ||--o| GURU_PEMBIMBING : "memiliki profil"
    USERS ||--o| PEMBIMBING_LAPANGAN : "memiliki profil (opsional)"

    TAHUN_AJARAN ||--o{ SISWA : "diikuti"
    TEMPAT_PKL ||--o{ PEMBIMBING_LAPANGAN : "memiliki"
    TEMPAT_PKL ||--o{ PENEMPATAN_PKL : "menjadi lokasi"

    SISWA ||--o{ PENEMPATAN_PKL : "memiliki"
    GURU_PEMBIMBING ||--o{ PENEMPATAN_PKL : "membimbing"
    PEMBIMBING_LAPANGAN ||--o{ PENEMPATAN_PKL : "membimbing"

    PENEMPATAN_PKL ||--o{ LOGBOOK : "menghasilkan"
    SISWA ||--o{ LOGBOOK : "mengisi"
    LOGBOOK ||--o{ DOKUMENTASI_LOGBOOK : "melampirkan"
    GURU_PEMBIMBING ||--o{ LOGBOOK : "memvalidasi"

    SISWA ||--o{ PENILAIAN : "dinilai"
    PEMBIMBING_LAPANGAN ||--o{ PENILAIAN : "memberi"
    PENILAIAN ||--o{ DETAIL_PENILAIAN : "terdiri dari"
    ASPEK_PENILAIAN ||--o{ DETAIL_PENILAIAN : "digunakan pada"

    USERS ||--o{ NOTIFIKASI : "menerima"

    USERS {
        int id PK
        string nama
        string email
        string username
        string password
        string role "siswa|guru_pembimbing|pembimbing_lapangan|admin"
        string foto
        boolean is_active
        datetime created_at
        datetime updated_at
    }

    SISWA {
        int id PK
        int user_id FK
        string nis
        string nisn
        string kelas
        string jurusan
        string no_hp
        text alamat
        int tahun_ajaran_id FK
        string status_pkl "belum_ditempatkan|aktif|selesai"
    }

    GURU_PEMBIMBING {
        int id PK
        int user_id FK
        string nip
        string no_hp
        string jurusan_ampu
    }

    TEMPAT_PKL {
        int id PK
        string nama_perusahaan
        text alamat
        string no_telp
        string email
        string bidang_usaha
        string penanggung_jawab
        int kuota
    }

    PEMBIMBING_LAPANGAN {
        int id PK
        int user_id FK "nullable, jika punya akun login"
        int tempat_pkl_id FK
        string nama
        string jabatan
        string no_hp
        string email
    }

    TAHUN_AJARAN {
        int id PK
        string nama_tahun_ajaran
        string semester "ganjil|genap"
        boolean is_active
    }

    PENEMPATAN_PKL {
        int id PK
        int siswa_id FK
        int tempat_pkl_id FK
        int guru_pembimbing_id FK
        int pembimbing_lapangan_id FK
        date tanggal_mulai
        date tanggal_selesai
        string status "aktif|selesai|dibatalkan"
    }

    LOGBOOK {
        int id PK
        int siswa_id FK
        int penempatan_pkl_id FK
        date tanggal_kegiatan
        time jam_mulai
        time jam_selesai
        text uraian_kegiatan
        text kendala
        string status "draft|menunggu_validasi|disetujui|revisi|ditolak"
        text catatan_guru
        int validated_by FK "guru_pembimbing_id, nullable"
        datetime validated_at
        datetime created_at
    }

    DOKUMENTASI_LOGBOOK {
        int id PK
        int logbook_id FK
        string file_path
        string file_type
        datetime uploaded_at
    }

    ASPEK_PENILAIAN {
        int id PK
        string nama_aspek
        text deskripsi
        int bobot
    }

    PENILAIAN {
        int id PK
        int siswa_id FK
        int pembimbing_lapangan_id FK
        decimal nilai_akhir
        text feedback
        string status "draft|final"
        date tanggal_penilaian
    }

    DETAIL_PENILAIAN {
        int id PK
        int penilaian_id FK
        int aspek_penilaian_id FK
        decimal nilai
    }

    NOTIFIKASI {
        int id PK
        int user_id FK
        string judul
        text pesan
        string jenis "reminder_logbook|validasi|penilaian|umum"
        boolean is_read
        datetime created_at
    }
```

### Catatan Bisnis Rule Penting (untuk implementasi Model/Validasi)

- Satu siswa hanya boleh punya **satu penempatan PKL berstatus `aktif`** dalam satu waktu.
- Logbook wajib terkait ke `penempatan_pkl` yang sedang aktif — cegah siswa mengisi logbook jika belum ditempatkan.
- `penilaian` yang sudah berstatus `final` tidak boleh diedit lagi (kunci form di controller & tambahkan konfirmasi di UI).
- Penamaan tabel database disarankan snake_case & lowercase, contoh: `users`, `siswa`, `tempat_pkl`, `penempatan_pkl`, `logbook`, `dokumentasi_logbook`, `aspek_penilaian`, `penilaian`, `detail_penilaian`, `notifikasi`, `tahun_ajaran`.

---
